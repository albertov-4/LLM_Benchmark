\section{Benchmark Output Parsing and Output Structure}
The benchmark execution pipeline is designed to produce a set of traceable and independently analyzable artifacts. All generated data is stored within the \texttt{Benchmark\_Framework/outputs} directory, which is partitioned to reflect the sequential stages of evaluation: generation, parsing, and scoring.

This separation of concerns is a core design principle. It allows for the independent diagnosis of different failure modes: a model might generate a correct plan in prose but fail due to a parsing error, or it might produce a syntactically valid but logically incorrect plan; note also that the folders are not created until the end of the run (if the model finds a valid plan) or at the $8^{th}$ iteration if the model wasn't able to find a valid plan.
By storing the output of each stage, we can distinguish between these cases.

The \texttt{outputs} folder is organized into three main subdirectories:
\begin{itemize}
    \item \textbf{raw}: Contains the direct output from the language model.
    \item \textbf{parsed}: Stores the structured data extracted from the raw text.
    \item \textbf{scored}: Holds the final validation results and performance metrics.
\end{itemize}

Each of these directories follows a consistent hierarchical structure that mirrors the benchmark's matrix:
\begin{center}
\texttt{<run\_id>/<model>/<protocol>/<domain>/<difficulty>/<instance>.json}
\end{center}
This structure ensures that for any given problem instance, there are three corresponding folders: raw, parsed, and scored.

\subsection{Raw}
The files in the \texttt{outputs/raw/} directory are the most direct record of the interaction with the LLM. Each JSON file contains:
\begin{itemize}
    \item The full prompt sent to the model, including the PDDL domain and problem definitions.
    \item The model's complete text response.
    \item Any available "chain-of-thought" or reasoning text generated by the model before the final plan.
    \item Metadata from the model provider, such as generation tokens and stop reasons.
\end{itemize}
This layer serves as the basics for what the model produced, before any interpretation or processing.

\subsubsection{Raw file Structure}
Each file in the \texttt{raw} directory, like the one shown in Listing~\ref{lst:raw_json_structure}, provides a comprehensive summary of the datas needed for further analysis.

\begin{lstlisting}[language=Python, caption={Structure of raw JSON file}, label={lst:raw_json_structure}]
    {
  "model_id": #LLM model selected,
  "task_id": #problem and file selected,
  "protocol_id": #iterative repair or direct plan,
  "task_family": #PDDL domain problem selected,
  "tier": #easy, medium or hard depending on the instance,
  "instance_id": #instance selected,
  "generation_time_seconds": #sum of the time for each iteration,
  "attempts": [
    {
      "iteration": #N. of current iteration,
      "generation_time_seconds": #time for the generation of the iteration,
      "messages": [ #used for the reasoning message and the text prompts
        {
          "role": "system" #define the role of whos receiving the message,
          "content": #Here we place our text prompt, for example:
          "You are a PDDL planning assistant.\n\nYour task is to produce a valid plan for the given PDDL domain and problem.\n\nGlobal rules:\n- Use only action schemas explicitly defined in the domain..."
        },
        {
          "role": "user",
          "content": "TASK FAMILY: block-grouping\nDIFFICULTY: easy\nINSTANCE: pfile1\n\nTASK FAMILY: block-grouping\n\nYou are solving a numeric PDDL planning problem..."
        }
      ],
      "generation": { #raw output generated by the LLM, example:
        "raw_text": "(move_block_left b1)\n(move_block_left b1)\n(move_block_left b1)\n(move_block_left b2)..."
        #following tokens feature insterted but not useful
          "prompt_tokens": null,
          "completion_tokens": null,
          "total_tokens": null
        },
        "latency_s": #time latency,
        "notes": [
          #notes if present
        ],
        "reasoning_text": #reasoning text given us by the LLM
        "We need to produce a plan moving blocks to satisfy goal: blocks b1,b2,b3,b4 must be at same location (both x and y equal). b5 must be at a different location..."
        "finish_reason": finish reason for example stop of numb of max iter reached,
        "stop_reason": #for example error or stop,
        "reached_max_tokens": false,
        "stream_complete": #true or false,
        "stream_error": #error or null,
        "partial_output": #true or false,
        "timed_out_by_job_limit": #true or false,
        "generation_time_seconds": #time used for generation
      }
    },
  ]
}

\end{lstlisting}


\subsection{Parsed}
The \texttt{outputs/parsed/} directory contains the results of the first processing step. The parser attempts to extract a structured PDDL plan from the model's raw text response. Each JSON artifact in this layer includes:
\begin{itemize}
    \item The extracted sequence of PDDL actions.
    \item The final plan formatted as a single block of text, ready for the validator.
    \item Any "chain-of-thought" actions if the model produced a separate reasoning plan.
    \item A record of any formatting issues or parsing errors encountered, which helps diagnose text extraction failures.
\end{itemize}
This layer bridges the gap between unstructured natural language and the formal PDDL syntax required for validation.

\subsubsection{Parsed file Structure}
Each file in the \texttt{parsed} directory, like the one shown in Listing~\ref{lst:parsed_json_structure}, provides a comprehensive summary of the datas needed for further analysis.

\begin{lstlisting}[language=Python, caption={Structure of parsed JSON file}, label={lst:parsed_json_structure}]
{
  "model_id": #LLM model selected,
  "task_id": #problem and file selected,
  "protocol_id": #ierative repair or direct plan,
  "task_family": #PDDL domain problem selected,
  "tier": #easy, medium or hard depending on the instance,
  "instance_id": #instance selected,
  "generation_time_seconds": #sum of all the times of the single iterations,
  "raw_output_path": #path of the raw output ot the same instance,
  "attempts": [ #various iteration attempts
    {
      "iteration": #N. of current iteration,
      "generation_time_seconds": #time for the generation of the iteration,
      "parsed_plan": {
        "raw": {
          "actions": [ 
            #actions extracted from raw file
          ],
          "format_issues": [ #types of format issues if there are any ],
          "contains_reasoning": #true or false,
          "source_kind": #in this case is raw file
        },
        "reasoning": { #specific reasoning view
          "actions": [
            #reasoning actions extracted from raw file
          ],
          "format_issues": [ #format issues, some examples
            "unknown_action_names_found",
            "actions_embedded_in_text",
            "ambiguous_compressed_action"
          ],
          "source_ref": {
            "artifact": "raw",
            "field": "generation.reasoning_text"
          }
        }
      }
    },
  ]
}

\end{lstlisting}



\subsection{Scored}
The final layer, \texttt{outputs/scored/}, contains the quantitative results of the evaluation. After a plan is parsed, it is passed to the external VAL validator and the resulting JSON files store:
\begin{itemize}
    \item The official validation result from VAL.
    \item A boolean \texttt{solved} flag indicating whether the plan successfully achieves the goal.
    \item The number of iterations or repair attempts used to find a valid solution (if iterative repair is active).
    \item Metrics related to the plan's quality, such as its length and any specific errors identified by the validator.
\end{itemize}

\subsubsection{Scored file Structure}
Each file in the \texttt{scored} directory, like the one shown in Listing~\ref{lst:scored_json_structure}, provides a comprehensive summary of the datas needed for further analysis.

\begin{lstlisting}[language=Python, caption={structure of a scored JSON file.}, label={lst:scored_json_structure}]
{
  "model_id": #LLM selected ,
  "task_id": #domain and instance selected,
  "protocol_id": #iterative repair or direct plan,
  "solved": #true or false,
  "iterations_used": #number of iterations,
  "metrics": {
    "validity_at_1": #if valid with just the first iteration,
    "validity_at_k": #if valid at which itaeration,
    "iterations_to_valid": #n. of iterations to obtain a valid plan,
    "repair_success": #yes or no if iterative repair is on,
    "plan_length": #N. of actions in the plan,
    "error_type": #if error happens which one
    "hit_iteration_limit": #Current limit is 8, if reached without valid plan this becomes true
  },
  "raw_output_path": #path of the raw output ot the same instance
  "parsed_output_path": #path of the parsed output ot the same instance
  "attempts": [
    {
        "iteration": #current iteration,
        "generation_time_seconds": #time used for the generation of teh iteration,
        "validation_result": {
        "valid": #true or false,
        "status": #status of the validation,
        "error_type": #type of error, ex: unsatisfied_goal,
        "feedback_text": #feedbakc text containing also validator output,
        "failed_step": null,
        "failed_action": null,
        "goal_satisfied": #true or false,
        "plan_length": #N of plan's actions,
        "validation_time_ms": #time needed for validation,
        "raw_validator_output": #fro example: executed successfully,
        "details": {
            #details of the validator, for example external or internal validator
        },
        "reasoning_first_valid_prefix_length": ,
        "reasoning_first_valid_plan_text": ,
        "reasoning_final_plan_valid": #true or false,
        "reasoning_extra_actions_after_first_valid": null,
        "reasoning_candidate_count": 5,
        "reasoning_valid_candidate_count": 0,
        "reasoning_selected_candidate_index": 3,
        "reasoning_selected_candidate_truncated": false,
        "feedback_to_next_iteration": 
      },
    }
  ]
}
\end{lstlisting}
