TASK FAMILY: farmland

You are solving a numeric PDDL planning problem.

The domain models farms and numeric worker allocations.
The numeric fluent x(farm) represents the current amount assigned to each farm.
The function cost may be changed by some actions, but it is not itself an action.

Use only the action schemas defined in the domain:
- move-fast
- move-slow

Action interpretation:
- move-fast moves resources from one farm to an adjacent farm.
  It requires enough resources at the source farm and changes numeric values.
- move-slow moves a smaller amount from one farm to an adjacent farm.
  It also requires enough resources at the source farm and changes numeric values.

Planning rules:
- Track numeric values after each action.
- Respect adjacency preconditions.
- Respect all numeric preconditions before applying an action.
- Ensure all numeric goal inequalities are satisfied at the end.
- Ensure every farm requirement in the goal remains satisfied.

Output rules:
- Output only executable actions.
- Do not output numeric assignments, numeric comparisons, goal conditions, effects, or intermediate states.
- Do not output expressions such as (= ...), (>= ...), (increase ...), or (decrease ...).
- Each line must be either (move-fast farmA farmB) or (move-slow farmA farmB), using only farm names from the problem.
