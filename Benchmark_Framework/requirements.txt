# Leonardo-compatible benchmark environment.
#
# Keep PyTorch pinned to CUDA 12.1. Unpinned torch may install newer CUDA 13
# wheels, which are not compatible with the Leonardo setup used for these runs.
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Do not add mamba-ssm here. On Leonardo it must be installed separately with:
#   pip install --no-cache-dir --no-deps --no-build-isolation mamba-ssm==2.2.4 -v
# A plain `pip install mamba-ssm` can upgrade torch/triton to incompatible CUDA wheels.

transformers==5.9.0
accelerate==1.13.0
huggingface_hub==1.17.0
tokenizers==0.22.2
safetensors==0.7.0
sentencepiece==0.2.1
pillow==12.2.0
PyYAML==6.0.3
openai==2.38.0
