% =====================================================================
%  benchmark_preamble.tex
%  Shared preamble for the LLM-PDDL benchmark report sections.
%  Put \input{benchmark_preamble} in your main.tex PREAMBLE
%  (i.e. before \begin{document}), or paste these lines there.
%  Written defensively so it can be merged into an existing project:
%   - colours are namespaced (bm*) so your blue/red are untouched
%   - hyperref / tcolorbox are only loaded if you haven't already
%   - \code is defined only if you don't already have one
% =====================================================================


% tcolorbox: guard the [most] option-load, then pull only the libraries we need
\makeatletter
\ifdefined\tcbuselibrary\else\usepackage{tcolorbox}\fi
\makeatother
\tcbuselibrary{skins,breakable}

% tikz
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,fit,backgrounds}

% hyperref only if your project hasn't loaded it; keep \texorpdfstring safe either way
\makeatletter
\@ifpackageloaded{hyperref}{}{\usepackage{hyperref}}
\makeatother
\providecommand{\texorpdfstring}[2]{#1}

% ---- colours (bm-prefixed; 'accent' is the teal) ----
\definecolor{accent}{HTML}{0F9D8F}
\definecolor{bmblue}{HTML}{2F6FB0}
\definecolor{bmred}{HTML}{C0492E}
\definecolor{amber}{HTML}{C47D1E}
\definecolor{ink}{HTML}{14181D}
\definecolor{codebg}{HTML}{F4F6F6}
\definecolor{linecol}{HTML}{D9DEE0}
\definecolor{commentcol}{HTML}{5D8D85}
\definecolor{muted}{HTML}{54616B}

% ---- inline code macro (only if you don't already define \code) ----
\ifdefined\code\else\newcommand{\code}[1]{\texttt{#1}}\fi

% ---- code listing style ----
\lstdefinestyle{bmpycode}{%
  basicstyle=\ttfamily\footnotesize, breaklines=true, columns=fullflexible,
  keepspaces=true, showstringspaces=false, frame=single,
  rulecolor=\color{linecol}, backgroundcolor=\color{codebg},
  framesep=5pt, xleftmargin=7pt, xrightmargin=4pt, aboveskip=8pt, belowskip=6pt,
  commentstyle=\color{commentcol}, keywordstyle=\color{bmblue},
  stringstyle=\color{bmred}, language=Python}
\lstset{style=bmpycode}

% ---- callout boxes ----
\newtcolorbox{bmkeybox}[1]{enhanced,breakable,colback=accent!4,colframe=accent!55,
  coltitle=ink,fonttitle=\bfseries\sffamily\small,title={#1},
  left=8pt,right=8pt,top=6pt,bottom=6pt,boxrule=0.4pt,
  borderline west={2.5pt}{0pt}{accent}}
\newtcolorbox{bmcavbox}[1]{enhanced,breakable,colback=amber!5,colframe=amber!55,
  coltitle=ink,fonttitle=\bfseries\sffamily\small,title={#1},
  left=8pt,right=8pt,top=6pt,bottom=6pt,boxrule=0.4pt,
  borderline west={2.5pt}{0pt}{amber}}
\newtcolorbox{bmformulabox}{colback=codebg,colframe=linecol,boxrule=0.4pt,
  left=10pt,right=10pt,top=7pt,bottom=7pt}
% ===================== end benchmark_preamble.tex =====================
